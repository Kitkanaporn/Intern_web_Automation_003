/schedule   xxx
workflow ชื่อ UAT_Notification 

โดยใช้เฉพาะไฟล์ที่ส่งไปเท่านั้ และห้ามใช้ข้อมูลจากแหล่งอื่น ใช้ไฟล์ต่อไปนี้เท่านั้น: 

AGENT.md สำหรับคำสั่งและภาพรวมของ workflow; 
SKILL.md สำหรับ logic หลัก Phase 0–5; 
shared_config.yaml สำหรับ config กลางของอีเมลและ schedule; 
project_config.yaml สำหรับ config ของ Jira project CSD; 
Map_User_Email.xlsx สำหรับ mapping ผู้ใช้และอีเมลจากชีต map; 
UAT_Template_Email.html สำหรับ template อีเมล อ่าน AGENT.md และ 
SKILL.md เพื่อโหลด logic 

การทำงาน 
อ่าน shared_config.yaml และ project_config.yaml เพื่อดึงค่าการตั้งค่าทั้งหมด เชื่อมต่อ Jira และดึง Epic issues ทั้งหมดที่มี 
UAT Start Date ภายใน 5 วันข้างหน้า หรืออยู่ในช่วง UAT ปัจจุบัน Cross-reference กับ Map_User_Email.xlsx 
เพื่อหา recipient จาก service fields ทำตาม Phase 1–5 ตามที่กำหนดไว้ใน SKILL.md โดยไม่ต้องขอ 
confirmation และให้ทำงานแบบอัตโนมัติเต็มรูปแบบ แสดงผลลัพธ์บนหน้าจอโดยตรงเท่านั้น และห้ามสร้างไฟล์ output ถ้ามี 
UAT ที่ต้องแจ้งเตือน ให้แสดง 2 cards ได้แก่ subject และ email body พร้อมกล่อง recipient, กล่อง CC
และเนื้อหาอีเมล โดยแต่ละส่วนต้องมีปุ่ม Copy ถ้าวันนี้ไม่มี UAT ที่ต้องแจ้งเตือน ให้แสดงข้อความว่าไม่จำเป็นต้องส่ง 
UAT notification วันนี้ หลังประมวลผลเสร็จ ให้ post Jira comment ว่า "UAT Notification Complete" บน ticket ทั้งหมดที่ยัง
